const router = require('express').Router();

module.exports = router;

//needs post route for creation of new posts - taken to dashboard after
//needs put route to update post - go to dashboard after update
//needs delete route to delete previous post-go back to dashboard after deleting