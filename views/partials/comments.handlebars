<div class="comments">
  <br>
  {{#each this}}
    <section id="comment-section" class="comment">
      <div class="meta">
        {{user.username}}
        ,
        {{format_date created_at}}
      </div>
      <div class="text">
        {{contents}}
      </div>
    </section>
  {{/each}}
</div>